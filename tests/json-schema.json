{
  "$defs": {
    "ParameterValue": {
      "properties": {
        "name": {
          "title": "Name",
          "type": "string"
        },
        "value": {
          "title": "Value",
          "type": "string"
        }
      },
      "required": [
        "name",
        "value"
      ],
      "title": "ParameterValue",
      "type": "object"
    },
    "PresetGroup": {
      "properties": {
        "type": {
          "description": "Type of configuration preset, e.g. configure, build, test.",
          "minLength": 1,
          "title": "Type",
          "type": "string"
        },
        "nameTemplate": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Template string for constructing preset names. By default, preset names are the group name followed by the parameter value names, separated by '-'.",
          "title": "Nametemplate"
        },
        "inherits": {
          "default": [],
          "description": "Name of pre-existing configuration presets to inherit in all generated presets.",
          "items": {
            "type": "string"
          },
          "title": "Inherits",
          "type": "array"
        },
        "parameters": {
          "additionalProperties": {
            "oneOf": [
              {
                "additionalProperties": {
                  "type": "string"
                },
                "type": "object"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "items": {
                  "$ref": "#/$defs/ParameterValue"
                },
                "type": "array"
              }
            ]
          },
          "description": "Parameters to generate presets from.",
          "minProperties": 1,
          "title": "Parameters",
          "type": "object"
        },
        "templates": {
          "default": {},
          "description": "Template for generating configuration options.",
          "title": "Templates",
          "type": "object"
        }
      },
      "required": [
        "type",
        "parameters"
      ],
      "title": "PresetGroup",
      "type": "object"
    }
  },
  "properties": {
    "version": {
      "const": 0,
      "title": "Version",
      "type": "integer"
    },
    "presetGroups": {
      "additionalProperties": {
        "$ref": "#/$defs/PresetGroup"
      },
      "description": "Preset groups. Presets are generated from the Cartesian product of the parameters in each group.",
      "title": "Presetgroups",
      "type": "object"
    }
  },
  "required": [
    "version",
    "presetGroups"
  ],
  "title": "Exploder",
  "type": "object"
}
